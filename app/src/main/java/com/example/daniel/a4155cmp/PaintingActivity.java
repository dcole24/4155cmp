package com.example.daniel.a4155cmp;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.CheckBox;

public class PaintingActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_painting);
    }

   /* public void onCheckboxClicked(View view) {
        // Is the view now checked?
        boolean checked = ((CheckBox) view).isChecked();

        // Check which checkbox was clicked
        switch (view.getId()) {
            case R.id.checkbox_Painting:
                if (checked)

            else
                // Remove the meat

            case R.id.checkbox_Painting2:
                if (checked)
                // Cheese me
            else
                // I'm lactose intolerant\
            case R.id.checkbox_Painting3:
                if (checked)
                    else

            case R.id.checkbox_Painting4:
                if (checked)
                // Cheese me
            else
                // I'm lactose intolerant\

            case R.id.checkbox_Painting5:
                if (checked)
                // Cheese me
            else
                // I'm lactose intolerant\
            case R.id.checkbox_Painting6:
                if (checked)
                // Cheese me
            else
                // I'm lactose intolerant\
                break;

        }*/
    }
